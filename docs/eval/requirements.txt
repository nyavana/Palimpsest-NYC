# Host-side eval harness deps. Kept out of apps/api/pyproject.toml so the
# runtime API image stays slim. Pin major versions; we don't need bleeding-edge.
httpx>=0.27,<0.28
PyYAML>=6.0,<7.0
matplotlib>=3.9,<4.0
redis>=5.0,<6.0
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<0.25
